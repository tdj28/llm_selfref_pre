# Signed dose-scan data boundary

This directory is the Git-visible control plane for
`consciousness_sae_signed_dose_scan_v1`.

Only the dated, prospective machine-plan directory and compact, independently
audited metadata may be tracked here. Canonical raw data remains on the RunPod
network volume under the distinct namespace:

`consciousness_sae_signed_dose_scan/consciousness_sae_signed_dose_scan_v1/raw`

Do not copy residual tensors, logits, row-level output, model or checkpoint
files, archives, or runtime logs into this repository. The predecessor's raw
namespace is not an input to this study.

The canonical prospectively frozen plan path is:

`dose_scan_v1_plan_20260716/`

Its manifest cannot authorize execution. A separate compact review
adjudication, passing smaller-model promotion receipt, live Git/source match,
and short-lived execution authorization are all required.

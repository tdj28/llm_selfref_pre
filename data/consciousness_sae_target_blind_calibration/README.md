# Pre-SAE target-blind calibration metadata

This Git namespace contains prospective machine plans and compact,
independently audited receipts and result indexes for
`consciousness_sae_target_blind_calibration_v2`.

The calibration uses fresh mundane prompts and generic deterministic layer-50
directions. It does not render the paper prompts, load target SAE features, or
pool predecessor rows. Predecessor compact receipts are bound only as disclosed
adaptive-design provenance.

Canonical residual arcs, intervention tensors, transport predictions, logits,
and row-level observations remain under the study's sentinel-bound namespace on
RunPod network volume `bv9gb9j32y`. Git stores hashes and compact audit evidence,
not the raw archive.

Plan directories are immutable evidence:

- `calibration_v2_plan_20260714` is the superseded candidate reviewed by the
  incomplete first Pro call. It is immutable reviewed evidence and was never
  authorized or executed.
- `calibration_v2_plan_20260714_r2` is the repaired candidate reviewed during
  the first adjudication pass. It is also immutable reviewed evidence, was
  never authorized or executed, and must not be overwritten or repurposed.
- `calibration_v2_plan_20260714_r3` is the sole active canonical path for the
  attempt-2/B01-repaired final design. It is immutable. Its raw transaction
  completed under run ID `calv2-r3-1a16572-20260715T002344Z`, and the final
  audit-only recovery completed under attempt ID
  `calv2-r3-audit-recovery-3a9a54d-20260716T202903Z` after the C15/E15/F15
  validation and review chain.

The tracked result index and small authoritative receipts are under
`docs/consciousness_sae_target_blind_calibration/results/`
`calv2-r3-audit-recovery-3a9a54d-20260716T202903Z/`. The large compact audit,
compact summary, and raw tensors remain on network volume `bv9gb9j32y`.

# Combined Adaptive Public-SAE Run

This directory combines the frozen long-form n=3 base with a disjoint n=17-per-cell precision extension. The extension was chosen after inspecting the base result and is therefore adaptive. Component raw outputs, judgments, logs, manifests, and runtime records remain in their original directories.

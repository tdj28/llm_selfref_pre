OPENAI_API_KEY="replace-with-openai-key"
ANTHROPIC_API_KEY="replace-with-anthropic-key"

# Required only for gated Llama downloads or agent-managed GPU runs.
# HF_TOKEN="replace-with-huggingface-token"
# RUNPOD_API_KEY="replace-with-runpod-key"

# Optional proprietary replication path; unavailable for the current release.
# GOODFIRE_API_KEY="replace-with-goodfire-key"
# STEERING_API_KEY="replace-with-steering-api-key"

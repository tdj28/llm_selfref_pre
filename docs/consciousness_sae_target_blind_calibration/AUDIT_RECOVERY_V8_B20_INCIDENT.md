# Audit-recovery V8 successor incident record

Status: **B17-B20 are historical stop-ships. No scientific result was produced.**

This document is prospective context for the C11/E11/F11 V8 successor. It does
not authorize a provider call or a recovery launch, and it contains no recovered
calibration values.

## Prior focused review (B17-B19)

The immutable focused GPT-5.6 Sol review in
`reviews/audit_recovery_bindingfix_gpt_pro_20260715_completed/` returned
`READY AFTER SPECIFIED FIXES` under response
`resp_0038445e5c1ea968016a57b46d6378819893a1587d331ce1a6`. Its stable findings
were B17-B19 and I10-I12. The complete five-file provider record is preserved
unchanged. A preceding paid attempt, response
`resp_0a3a9f471779e79c016a57b366162481988f0d8b2d3f04e061`, ended incomplete at
`max_output_tokens`; it supplied no adjudicable review. Both calls must remain
in cumulative cost/call disclosure.

B17 identified a SOURCE/ACTIVE bootstrap binding mismatch. B18 required
independently inspectable cleanup and zero-artifact evidence for the failed
attempt. B19 required execution to be bound to the exact reviewed controller
bytes. The compact B18 closure in `reviews/audit_recovery_b18_cleanup_closure/`
records the exact failed pod, its empty designated output, and exact-pod
termination without importing the multi-megabyte retrieved tree into Git.

## B20 final-confined failure

The next owned B200 attempt was
`calv2-r3-audit-recovery-2479ed0-20260715T165648Z` on pod
`eeo1skjkwjqot5`. Issue-time authorization completed, but the first final
confined review validation invoked Git from the exact ACTIVE closure. Git first
failed while opening `/dev/null` read/write:

`fatal: could not open '/dev/null' for reading and writing: Permission denied`

That observed error was only the first symptom. ACTIVE intentionally contains
exactly `RECOVERY_BOUND_PATHS` and no `.git`; allowing `/dev/null` would merely
have exposed the structural next failure, `not a git repository`. The correct
repair is therefore not a new device-write exception. All live Git ancestry and
diff checks run during issue from SOURCE, their final HEAD is sealed into the
self-hashed authorization, and final-confined validation repeats the complete
byte/semantic review validation without invoking Git.

The compact B20 closure in
`reviews/audit_recovery_b20_gitless_active_incident/` binds the 71-file retrieved
attempt inventory, the 6.8 MB authorization by physical SHA-256, the exact
Landlock-only designated output, the traceback, launch gate, ownership, and
termination chain. The large authorization and retrieved tree remain in the
private/network evidence archive and are not committed.

Mechanically established facts:

- issue authorization receipt: `f6d0fa7fdf5b6ec8553fce2fe8df7842dd28f5a63fb5a9674a6358d4af152358`;
- authorization physical SHA-256: `897a0fe5fac8e898f6367b8115a982a7580c0224843a76e2514589f6277274a7`;
- portable B20 closure receipt: `3f5dee0ccbef6af18302667c0cea95be0c7e4c4cd6d1f9b0d61a4222c1e157d9`;
- omitted private-archive closure receipt anchor: `61f1263e517b179ec3515425d82cd6918b8ab1317b80933bebd2bd669d940aed`;
- designated output contained only `LANDLOCK_ENFORCEMENT.json`;
- `ATTEMPT_STARTED.json`, `FAILURE.json`, compact audit, summary, and publication
  marker were absent;
- zero model forwards and no scientific result;
- the exact owned pod was deleted and unrelated account inventory was unchanged.

## V8 successor boundary

The successor must preserve V7 as immutable historical evidence, bind fresh
C11 local and disposable-B200 qualification receipts in the V8 input directory,
submit exactly one cumulative GPT-5.6 Sol review including the B17 and B20
records, and permit F11 to add only the five provider files plus structured
adjudication. Any new blocker B21 or later stops the launch. Final ACTIVE remains
repository-free and strict Landlock grants no `/dev/null` write exception.

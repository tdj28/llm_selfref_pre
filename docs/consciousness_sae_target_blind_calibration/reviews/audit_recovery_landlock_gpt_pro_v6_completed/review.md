# Verdict

The packet is unusually strong on exact-byte lineage, narrow claim scope, failure preservation, and independent offline verification. On the supplied evidence, the proposed recovery remains a technical re-audit of an already completed raw model transaction—not a new intervention or model transaction—and the only scientific compatibility change remains the target-independent J-map inventory predicate.

The two newly disclosed stop-ships have been repaired with appropriately minimal changes:

- **B14:** Restoring the two immutable r3 runtime files byte-for-byte, moving `pytest==8.4.2` into separately bound qualification-only files, and adding the unmocked 41-file authentic plan/provenance gate is the smallest sound repair. The current supplied `requirements-runpod-b200.txt` and `setup_runpod_guest.sh` match the r3 sizes and hashes in `source_files.json`. The qualification wrapper is referenced by the qualification controller but is absent from the final confined child command; it is present-and-unused in the bound final closure rather than invoked during recovery.
- **B15:** Moving only the attempt parent to `/workspace/csae`, shortening the socket leaf to `.s`, and enforcing a 91-byte operational ceiling in the producer, launcher, and independent verifier is the smallest sound repair. The supplied paths derive to 91 bytes for preflight and 90 bytes for execution, with a 16-byte reserve below the 107-byte Linux pathname maximum. Tests cover multibyte path counting and over-budget rejection.

The C9 evidence is current rather than a relabeling of C6 or C8 evidence. The supplied fresh local and target receipts both identify code-freeze commit `b404491fe4bd28931e45bed16fb5d7d9a27382f5`, source/test inventory SHA-256 `881f37faf70f14c973bddb1268dfd10888ba071bd9483755c00142634d59e9d6`, 216 collected tests, and zero failures or not-run tests; the target receipt records all 216 as passed. The target Landlock receipt shows pipe-backed standard streams and writable pipe descriptors rather than inherited writable regular files, while retaining the unchanged fail-closed descriptor audit. Its CUDA receipt reports the exact C9 30-file closure, zero model forwards, zero Torch module calls, zero target renders, zero target feature vectors, and successful BF16 CUDA arithmetic and synchronization. These are quoted receipt contents; no claim is made here to have inspected a repository, provider account, or network volume outside the supplied artifacts.

The claim boundary is adequately narrow:

- The primary scientific scope remains descriptive performance on the exact eight-prompt, three-direction fixed panel at layer 50 and dose 0.03.
- The eight prompts—not the J artifact’s 125 fitting prompts, 120 signed pairs, 4,872 transport rows, or 68 orientation fixtures—remain the independent units.
- The bootstrap remains a fixed-panel prompt-resampling stability calculation, not a prompt-population confidence interval.
- No fresh model forward is authorized.
- The Landlock claim is limited to handled process-tree filesystem content/topology mutations, with the `/proc/self/task` and exact NVIDIA-device exceptions expressly disclosed.
- Endpoint rehash equality is not represented as continuous external immutability.
- The original model transaction, failed recovery host, qualification host, and future recovery host remain temporally and operationally distinct.

The long-context accounting is internally consistent. From the supplied stored usage, v4 reconstructs to `$12.555555` and v5 to `$15.121205` at the disclosed long-context rates; both remain below their respective `$25` authorizations and are correctly labeled retrospective reconstructions rather than invoices. The prospective 600,000-token/20,000-output-token envelope with 5.0/2.2 reserve multipliers yields the stated `$69.48` worst-case reserve, covered by the `$75` authorization.

No genuinely new blocker B16 or later and no genuinely new important finding I10 or later is identified. Readiness applies only to the exact packet inventory supplied here, including plan SHA-256 `6b9d30d64c8f54be1a330c9d4c4cd6bf8c4cf69af8e9462c556e22601782a07f`, current source/test bytes, C9 receipts, historical v5 context, and the B14/B15 incident record. Any changed packet byte, regenerated receipt, revised adjudication, source edit, test edit, or substituted qualification evidence requires another separately authorized exact-byte review.

READY TO FREEZE

# Blocking findings

**Current blocking findings: none.**

The required historical dispositions are retained below so that no identifier disappears or is silently reinterpreted.

## B01 — Scientific-equivalence closure

- **Severity:** Historical blocker; closed on the current packet.
- **Plan section or excerpt:** “The outcome-blind `AUDIT_RECOVERY_SCIENTIFIC_EQUIVALENCE.{json,md}` appendix mechanically binds the frozen plan/source bytes…”
- **Why it matters:** A post-run compatibility correction must not silently alter hook semantics, positions, J transport, readout, aggregation, bootstrap, thresholds, or claim gates.
- **Concrete minimum fix:** Already implemented: retain the machine and Markdown appendices, affirmative scientific-field projection, exact frozen source hashes, filtered required maps, and exactly one `audit.audit` invocation.
- **Claim affected:** Scientific equivalence of the recovered audit apart from the required-subset loader correction.
- **Disposition:** Fixed. The supplied machine appendix is in the exact packet and binds the frozen scientific closure without opening raw or compact outcomes.

## B02 — Code executing before confinement and guards

- **Severity:** Historical blocker; closed.
- **Plan section or excerpt:** Direct `python -B -E -s -S /absolute/path/...` launcher and same-PID bootstrap.
- **Why it matters:** `site`, `.pth`, package initialization, or an unconstrained child restart could execute before Landlock and zero-forward guards.
- **Concrete minimum fix:** Already implemented: preserve direct-script startup, all four interpreter flags, forbidden-environment checks, complete import-root inventory, guard priming before project import, and same-PID `execve`.
- **Claim affected:** Startup determinism, leakage prevention, executable closure, and scoped zero-forward evidence.
- **Disposition:** Fixed. Current target evidence records the guarded bootstrap and successful same-PID Landlock path.

## B03 — Historical and recovery campaign clocks conflated

- **Severity:** Historical blocker; closed.
- **Plan section or excerpt:** “The recovered audit preserves the original top-level `campaign_started_at_unix`, `campaign_deadline_at_unix`, and `hourly_price_usd` fields…”
- **Why it matters:** Replacing original fields with recovery timing would misrepresent a later audit as part of the original model transaction.
- **Concrete minimum fix:** Already implemented: retain original top-level fields, add `original_execution_campaign` and a separate `recovery_execution_campaign`, and bind publication to `recovery_deadline_at_unix`.
- **Claim affected:** Temporal provenance and separation of execution from recovery.
- **Disposition:** Fixed.

## B04 — Required-subset correction contradicted by an exact inventory whitelist

- **Severity:** Historical blocker; closed.
- **Plan section or excerpt:** Recovery loader requires `set(required) <= set(available)` and passes a filtered mapping.
- **Why it matters:** An exact `0..78` whitelist would be broader than necessary and contradict the stated compatibility correction; passing all maps downstream could alter behavior or metadata.
- **Concrete minimum fix:** Already implemented: normalize keys, reject duplicates and missing required layers, record all extras in recovery provenance, and pass exactly layers 45–78 downstream.
- **Claim affected:** Narrowness of the J-checkpoint correction and comparability to r3.
- **Disposition:** Fixed.

## B06 — Recovery inventory fields entered scientific J metadata

- **Severity:** Historical blocker; closed.
- **Plan section or excerpt:** The loader returns scientific metadata containing only `sha256`, required `map_count=34`, and `revision`.
- **Why it matters:** Adding the available 79-map inventory to `artifact_recomputation.j_lens` would change an affirmatively projected scientific field.
- **Concrete minimum fix:** Already implemented: keep the full inventory only under `recovery_audit.j_checkpoint_inventory`.
- **Claim affected:** Byte-semantic scientific-output equivalence.
- **Disposition:** Fixed.

## B07 — Negative verdict could satisfy a substring readiness test

- **Severity:** Historical blocker; closed.
- **Plan section or excerpt:** `_terminal_review_verdict` requires one recognized terminal line in the single Verdict section.
- **Why it matters:** `NOT READY TO FREEZE` contains the substring `READY TO FREEZE`.
- **Concrete minimum fix:** Already implemented: retain exact terminal-verdict parsing, duplicate-verdict rejection, and structured adjudication.
- **Claim affected:** Legitimacy of review closure and authorization.
- **Disposition:** Fixed.

## B08 — Test receipts were not bound into authorization and verification

- **Severity:** Historical blocker; closed.
- **Plan section or excerpt:** Local and target receipts plus three qualification support receipts are review-, authorization-, recovery-, and verifier-bound.
- **Why it matters:** Source hashes alone do not establish execution of the exact tests on a compatible Linux/B200/Landlock host.
- **Concrete minimum fix:** Already implemented: retain code-freeze, complete source/test inventory, command, dependencies, node outcomes, and support-file physical and self-receipt hashes.
- **Claim affected:** Exact-byte test provenance and target-host compatibility.
- **Disposition:** Fixed for C9. The current receipts share code freeze `b404491fe4bd28931e45bed16fb5d7d9a27382f5` and source/test inventory `881f37faf70f14c973bddb1268dfd10888ba071bd9483755c00142634d59e9d6`.

## B09 — Producer and verifier cost ceilings differed

- **Severity:** Historical blocker; closed.
- **Plan section or excerpt:** Prospective producer and verifier now use the same `$75.00` v6 ceiling.
- **Why it matters:** Divergent ceilings could produce an authorization that the independent verifier must reject.
- **Concrete minimum fix:** Already implemented: retain symmetric constants and exact-boundary/over-boundary tests.
- **Claim affected:** Budget consistency and offline admissibility.
- **Disposition:** Fixed.

## B10 — Qualification timing did not support the recovery window

- **Severity:** Historical blocker; closed as feasibility evidence, with a live gate retained.
- **Plan section or excerpt:** Historical timed qualification reached readiness at host age 958 seconds, leaving 2,642 seconds and an 842-second reserve surplus.
- **Why it matters:** The one-shot recovery must not be authorized after staging and the full public-cache rehash leave too little time to finish.
- **Concrete minimum fix:** Already implemented: preserve the 1,800-second minimum-remaining gate and require a fresh `final_recovery` preflight on the actual recovery pod.
- **Claim affected:** Operational feasibility and one-shot authorization.
- **Disposition:** Fixed. Historical timing is feasibility evidence only and cannot waive the actual fresh-host stop condition.

## B11 — “Precise” confinement claim omitted `/proc/self/task`

- **Severity:** Historical blocker; closed.
- **Plan section or excerpt:** The precise claim now identifies both `/proc/self/task` `WRITE_FILE|TRUNCATE` and exact NVIDIA character-device `WRITE_FILE` exceptions.
- **Why it matters:** Omitting a real write exception would overstate the confinement guarantee.
- **Concrete minimum fix:** Already implemented: retain both exception classes and all ABI-4 limitations in the plan, receipts, and verifier.
- **Claim affected:** Filesystem-confinement interpretation.
- **Disposition:** Fixed.

## B12 — Machine equivalence JSON omitted from the reviewed-byte closure

- **Severity:** Historical v4 blocker; closed.
- **Plan section or excerpt:** The v5 and cumulative packet include `AUDIT_RECOVERY_SCIENTIFIC_EQUIVALENCE.json`.
- **Why it matters:** The central B01 evidence could otherwise change after review while only its generator tests remained bound.
- **Concrete minimum fix:** Already implemented: keep the machine JSON in the packet, source/test and packet-diff closures, and exact-regeneration test.
- **Claim affected:** Exact-byte scientific-equivalence evidence.
- **Disposition:** Fixed.

## B13 — Reserved identifier appearing only in “no new finding” text

- **Severity:** Historical pseudo-finding; not a substantive defect.
- **Plan section or excerpt:** Historical v5 review stated that no new blocker B13 or later existed.
- **Why it matters:** Automated ID extraction surfaced the identifier even though no B13 finding was made.
- **Concrete minimum fix:** None; retain the adjudicated rejection so the identifier is not silently dropped.
- **Claim affected:** Finding-lineage completeness only.
- **Disposition:** Rejected as a substantive finding.

## B14 — Qualification dependency edits broke immutable r3 source validation

- **Severity:** Historical stop-ship; definitively repaired on the current bytes.
- **Plan section or excerpt:** The v5 issue path found modified `requirements-runpod-b200.txt` and `setup_runpod_guest.sh`.
- **Why it matters:** The authentic authorization path could not validate the immutable r3 plan; using an older or locally edited worktree would break review and Git lineage.
- **Concrete minimum fix:** Implemented exactly:
  1. Restore the runtime requirements file to 204 bytes and SHA-256 `4796c2817460bae757dcbae4c141bca460100fe80b13eb888776270d8df4b806`.
  2. Restore the runtime setup script to 1,003 bytes and SHA-256 `f420180faf5c229439e4bf626ec05f5e9a10902508e62dbcef36f48abc1ab8fa`.
  3. Move pytest to `requirements-runpod-b200-qualification.txt` and `setup_runpod_qualification_guest.sh`.
  4. Retain the real 41-file plan/provenance validation gate.
- **Claim affected:** Exact r3 plan identity, authorization validity, and branch lineage.
- **Disposition:** Fixed. This is the smallest sound repair. The final recovery command does not invoke the qualification setup wrapper.

## B15 — Production AF_UNIX canary paths exceeded Linux limits

- **Severity:** Historical production stop-ship; definitively repaired on the current bytes.
- **Plan section or excerpt:** C6 production candidates were 218 and 217 bytes; the repaired candidates are 91 and 90 bytes.
- **Why it matters:** The canary bind would fail on pathname length before reaching the expected Landlock `EACCES`, making production execution impossible despite a successful shorter qualification path.
- **Concrete minimum fix:** Implemented exactly:
  1. Use attempt parent `/workspace/csae`.
  2. Use socket leaf `.s`.
  3. Freeze maximum 107, reserve 16, and operational budget 91 bytes.
  4. Enforce the contract independently in producer, launcher, and verifier.
  5. Test exact production lengths, multibyte counting, and over-budget rejection.
- **Claim affected:** Production feasibility and validity of the Unix-socket denial control.
- **Disposition:** Fixed. This is the smallest sound repair; no relative bind, abstract socket, symlink alias, working-directory change, or scientific path change is introduced.

# Important non-blocking findings

**New important findings I10 or later: none.**

## I01 — Scope of zero-forward evidence

- **Severity:** Historical important finding; resolved by bounded wording and layered controls.
- **Plan section or excerpt:** “This is not an OS-wide detector for arbitrary bespoke native callables, sibling processes, or device `ioctl` effects.”
- **Why it matters:** Torch/Transformers guards cannot establish absence of every hypothetical native model implementation outside the approved closure.
- **Concrete minimum fix:** Already implemented: retain exact import roots, static runner exclusion, startup guards, Torch and Transformers guards, inner counters, and target-free CUDA receipts.
- **Claim affected:** Zero new model forwards in the approved recovery process and closure.
- **Disposition:** Resolved.

## I02 — Endpoint equality is not continuous immutability

- **Severity:** Historical important finding; resolved.
- **Plan section or excerpt:** Pre/post hashes do not exclude intermediate changes by sibling processes or other NFS clients.
- **Why it matters:** Endpoint equality and process-tree Landlock confinement answer different questions.
- **Concrete minimum fix:** Retain the current endpoint-only language and avoid “read-only mount” or “continuous immutability.”
- **Claim affected:** Raw and provenance integrity.
- **Disposition:** Resolved.

## I03 — Structured adjudication and stable IDs

- **Severity:** Historical important finding; resolved.
- **Plan section or excerpt:** Findings require stable IDs, blocking flags, dispositions, rationales, and changed-path sets.
- **Why it matters:** Findings could otherwise disappear or be reinterpreted after packet changes.
- **Concrete minimum fix:** Retain the cumulative adjudication schema and reserved-ID checks.
- **Claim affected:** Review lineage and frozen decisions.
- **Disposition:** Resolved.

## I04 — Exact local and target execution evidence

- **Severity:** Historical important finding; resolved for C9.
- **Plan section or excerpt:** Exactly two self-hashed test receipts plus three target support receipts are bound.
- **Why it matters:** Local macOS skips cannot substitute for Linux/Landlock/CUDA execution.
- **Concrete minimum fix:** Retain the exact C9 receipts and support files; do not substitute C6 or C8 evidence.
- **Claim affected:** Test provenance and target compatibility.
- **Disposition:** Resolved. The current target receipt records 216/216 passed and no skips.

## I05 — Third-party reproduction boundary

- **Severity:** Historical important finding; adequately limited.
- **Plan section or excerpt:** Raw tensors remain on the network volume; the retrieved bundle is independently verifiable offline.
- **Why it matters:** A third party without the raw, public-artifact cache, and historical provenance bytes cannot recompute every metric from public Git artifacts alone.
- **Concrete minimum fix:** Continue using “offline-verifiable” or “receipt-verifiable,” not “public end-to-end reproduction.”
- **Claim affected:** Reproducibility.
- **Disposition:** Resolved by claim limitation.

## I06 — Independent units versus repeated rows

- **Severity:** Historical important finding; resolved.
- **Plan section or excerpt:** Independent unit `prompt_id`; eight fixed units.
- **Why it matters:** Directions, doses, branches, readout layers, transports, orientation fixtures, and the artifact’s 125 fitting prompts do not increase the independent study-unit count.
- **Concrete minimum fix:** Retain prompt-level aggregation and resampling and the fixed-panel interval label.
- **Claim affected:** Uncertainty, power, estimand interpretation, and generalization.
- **Disposition:** Resolved.

## I07 — Runtime margin is a gate, not a guarantee

- **Severity:** Historical important finding; resolved operationally.
- **Plan section or excerpt:** Authorization requires at least 1,800 seconds remaining.
- **Why it matters:** A future recovery host can be slower than any prior qualification.
- **Concrete minimum fix:** Stop before authorization whenever the fresh pod has less than 1,800 seconds remaining; do not extend or reset the clock.
- **Claim affected:** Feasibility and one-shot execution.
- **Disposition:** Resolved.

## I08 — Fixed-panel and multiplicity limitations

- **Severity:** Historical important finding; resolved as a mandatory claim boundary.
- **Plan section or excerpt:** Eight prompts, sole primary layer 50, primary dose 0.03, layers 51–78 descriptive, no formal multiplicity adjustment.
- **Why it matters:** The large number of repeated rows must not be converted into population-level evidence or post hoc layer selection.
- **Concrete minimum fix:** Retain the fixed-panel descriptive claim, conjunctive gates, and no-population-generalization language.
- **Claim affected:** Construct validity and prevention of overstatement.
- **Disposition:** Resolved.

## I09 — Reserved identifier appearing only in “no new finding” text

- **Severity:** Historical pseudo-finding; not substantive.
- **Plan section or excerpt:** Historical v5 review stated that no new important finding I09 or later existed.
- **Why it matters:** Mechanical ID extraction surfaced the token despite the absence of an actual finding.
- **Concrete minimum fix:** None; retain its explicit adjudicated rejection.
- **Claim affected:** Finding-lineage completeness only.
- **Disposition:** Rejected as a substantive finding.

# What should remain unchanged

1. **Narrow J-loader correction**
   - Keep the pinned checkpoint SHA-256 `335056c17f0c24053c8c8c1eff168ef49e5a62ca590ffc29c84cf352ecd3ab03`.
   - Require `n_prompts=125`, `d_model=8192`, and all layers 45–78.
   - Reject noncanonical and duplicate-normalized layer identifiers.
   - Pass exactly the 34 required maps downstream.
   - Record all available and unused extra maps only in recovery provenance.
   - Do not replace subset semantics with an exact `0..78` whitelist.

2. **Scientific entry point and projection**
   - Invoke the frozen `audit.audit` once.
   - Keep monkeypatching limited to the J loader, recovery watchdog, and historical-time external-receipt validator.
   - Keep original `audit.py` SHA-256 `271f4f17a5ed66eaff43dc63f5a02d7ce45cdfd4a3c6a5b5c03bac33cf96a465`.
   - Preserve the affirmative scientific-field projection and exact regeneration test.

3. **Hook and state-coordinate semantics**
   - Edit `model.model.layers[50]` output at the zero-based block-50 post-block/pre-block-51 boundary.
   - Retain capture-before-edit hook order and one hook firing per edited continuation.
   - Retain `hidden_state[0,0,:]` with shape `[1,1,8192]`.
   - Retain the explicit post-edit block-50 output as the primary J source coordinate.
   - Retain post-block outputs 51–78 as later source coordinates.
   - Retain target coordinate block 79 output/final RMSNorm input.

4. **Tokenization and temporal carryover**
   - Keep the exact model revision and tokenizer files.
   - Render the exact chat template with generation prompt.
   - Use `token_ids[0:-1]` for the prefix and `token_ids[-1]` as the one-token continuation.
   - Keep clean and signed branches on equal prefix values but independent cache clones.
   - Reuse no generated continuation text.
   - Keep pre-edit equality and layers 45–49 upstream equality checks.

5. **Readout and sign conventions**
   - Keep signed central differences `(plus-minus)/2`.
   - Keep common mode `(plus+minus)/2-clean`.
   - Keep J application as `residual_delta @ J_l.T`.
   - Keep final RMSNorm and selected LM-head token readouts.
   - Keep signed-final-midpoint centering for predicted logit contrasts.

6. **Controls and falsification logic**
   - Wrong-orientation control.
   - Identity transport.
   - Five independently seeded random-J transports.
   - BF16 production versus FP32 J shadow.
   - Native BF16 post-edit byte equality.
   - Signed common-mode control.
   - Hard failure on missing, duplicate, unmanifested, nonfinite, or partial data.

7. **Statistical boundaries**
   - Eight fixed `prompt_id` units.
   - Three directions averaged within prompt.
   - Prompt-level bootstrap with 20,000 fixed replicates.
   - Primary layer 50 and primary dose 0.03 only.
   - Layers 51–78 descriptive only.
   - No imputation, outcome-based exclusion, optional stopping, or across-layer selection.
   - No prompt-population confidence interval or increased-power claim.

8. **Temporal provenance**
   - Keep the original model transaction, first blocked recovery host, C6/C7 qualification history, current C9 qualification evidence, and future recovery host distinct.
   - Keep original campaign fields unchanged.
   - Put fresh authority only in `recovery_execution_campaign`.
   - Continue validating the original authorization at its historical completion time rather than treating it as current authority.

9. **B14 repair**
   - Keep the two r3 runtime files byte-identical to `source_files.json`.
   - Keep pytest in the two qualification-only files.
   - Keep the authentic 41-file pre-GPU plan/provenance gate.
   - Keep the qualification wrapper absent from the final recovery command.

10. **B15 repair**
    - Keep `/workspace/csae`.
    - Keep `.s`.
    - Keep 107-byte Linux maximum, 16-byte reserve, and 91-byte operational budget.
    - Keep exact 91-byte preflight and 90-byte execution derivations.
    - Keep independent producer/launcher/verifier enforcement and byte-count tests.

11. **Direct startup and executable isolation**
    - Absolute direct scripts, never `-m`, for launcher and confined bootstrap.
    - `-B -E -s -S`.
    - Single-thread launcher before project or ML imports.
    - Complete active/dependency root manifests and ordered `sys.path`.
    - Historical provenance tree non-importable and separate from the active root.
    - Runtime shim instead of the model runtime.

12. **Landlock policy**
    - Handled mask `0x7ff2`.
    - Two exact output rules with `0x1b2`.
    - Exact `/proc/self/task` rule with `0x4002`.
    - Exact identity-bound NVIDIA character-device rules with `0x2`.
    - No wildcard, `/dev` directory rule, broader `/proc` rule, mount claim, or unconfined fallback.

13. **Descriptor and mapping audit**
    - Reject inherited writable regular files/directories even under the durable output root.
    - Reject raw, provenance, canary, and NVIDIA-device descriptors.
    - Reject unenumerated NVIDIA descriptors, non-stdio writable devices, and `io_uring`.
    - Reject every shared file-backed mapping.
    - Continue allowing pipe-backed standard streams and non-filesystem pipe descriptors as recorded in the C9 receipt.

14. **Two-canary controls**
    - Preconfinement writability baseline.
    - Full protected-denial matrix.
    - Output allow/deny matrix.
    - Exact expected `EACCES` and `EXDEV` outcomes.
    - Empty output canary at completion.
    - Protected canary byte/topology equality.

15. **One-shot failure semantics**
    - Landlock receipt consumes authority before `execve`.
    - Attempt marker is exclusive and commit-scoped.
    - No alternate output namespace or retry under the same authority.
    - No compact success if any gate fails.
    - Catchable post-marker failures produce an exclusive failure receipt.
    - A success bundle cannot coexist with `FAILURE.json`.

16. **Endpoint integrity and offline verification**
    - Rehash raw and historical provenance file and directory inventories before and after the audit.
    - Do not upgrade endpoint equality to continuous immutability.
    - Keep the standard-library-only, network-free verifier.
    - Write the verifier receipt outside the retrieved bundle.
    - Release no scientific claim before verifier success.

# Minimal revised design

No packet-changing design revision is required. The minimal executable design is:

1. **Freeze this exact packet and adjudication**
   - Bind the supplied source, tests, plan inventories, C9 receipts, historical v5 evidence, and B14/B15 incident evidence.
   - Require `C9 <= E9 <= F9`.
   - Require no source/test diff from C9 to F9.
   - Require no reviewed-packet diff from E9 to F9.
   - Treat any changed byte as requiring a new review.

2. **Run the authentic pre-GPU issue gate before provisioning**
   - Validate the canonical r3 plan.
   - Derive all 41 historical provenance paths.
   - Require inventory SHA-256 `ff02d92e681e662261b57dab00882a654eaf7b0d505dd2f210ab06f57ba8bd74`.
   - Confirm the two canonical runtime files retain their r3 hashes.
   - Confirm the qualification wrapper is not part of the final command.

3. **Create one distinct fresh recovery pod**
   - One B200 in `US-CA-2`.
   - Volume `bv9gb9j32y`.
   - Fresh ownership, guest, and cache receipts.
   - Distinct from the C9 qualification pod and all historical recovery pods.
   - One provider-creation-bound 60-minute/$6 recovery envelope.

4. **Stage the exact final closure**
   - Use `/workspace/csae/<48-byte-attempt-id>`.
   - Inventory every active and dependency import-root byte.
   - Keep the historical provenance tree non-importable.
   - Rehash all 45 public-artifact files and 156,023,372,845 bytes.
   - Resolve and identity-bind the current host’s exact NVIDIA device files.

5. **Run the actual-host `final_recovery` preflight**
   - Use the exact 91-byte preflight socket path.
   - Run the two-canary Landlock matrix.
   - Run direct no-site bootstrap and guarded BF16 CUDA arithmetic.
   - Require zero model forwards, zero target renders, and zero target feature vectors.
   - Stop before authorization if any evidence differs or fewer than 1,800 seconds remain.

6. **Issue exactly one authorization**
   - Bind the fresh pod, receipts, closure, import manifest, current device identities, command, attempt namespace, deadline, budget, review, and C9 test evidence.
   - Permit no model forward and no retry.

7. **Execute through the direct launcher**
   - Audit threads, inherited descriptors, and mappings.
   - Install Landlock.
   - Run the 90-byte execution canary socket test.
   - Publish the exclusive Landlock receipt.
   - Same-PID `execve` the guarded bootstrap.
   - Validate authority and claim the exclusive attempt marker.

8. **Run the frozen scientific audit once**
   - Rehash raw and provenance trees.
   - Apply only the required-layer subset loader.
   - Pass exactly maps 45–78 to the frozen auditor.
   - Recompute the complete frozen audit.
   - Rehash both protected trees again.
   - Publish the compact pair and recovery publication marker atomically, or publish no success.

9. **Retrieve, terminate, and verify**
   - Retrieve success or failure evidence.
   - Delete the exact owned recovery pod.
   - Preserve the network volume and raw namespace.
   - Run the supplied offline verifier.
   - Release only the fixed-panel, disclosed post-run technical-recovery claim.

# Freeze checklist

- [ ] Packet plan SHA-256 remains `6b9d30d64c8f54be1a330c9d4c4cd6bf8c4cf69af8e9462c556e22601782a07f`.
- [ ] Current machine-equivalence JSON physical SHA-256 remains `f0b4f404dfc6a200632a3082eefec5024a346750a08c3a9ed8a66ab5e8f1c2a0`.
- [ ] Current recovery source SHA-256 remains `ee1f328fa9408430920fb48537b31f9abd6ced6ac7eda44559d267768bf50b7a`.
- [ ] Current launcher SHA-256 remains `094c4a6081fe782939ad390205cdd350846e00a5fd2a382168405f4c27ed9a03`.
- [ ] Current offline verifier SHA-256 remains `05f6b1de0876e3c7704a6d91844dfd1012eb00bde99ee006b7fd13468b0d9685`.
- [ ] C9 code-freeze commit remains `b404491fe4bd28931e45bed16fb5d7d9a27382f5`.
- [ ] C9 local and target receipts both retain source/test inventory SHA-256 `881f37faf70f14c973bddb1268dfd10888ba071bd9483755c00142634d59e9d6`.
- [ ] C9 local receipt physical SHA-256 remains `af78f32aff34c44b7b3f893e7a61ed4581d19750a0fe75b026bc8c76aca8e824`.
- [ ] C9 target receipt physical SHA-256 remains `49dec25836903dedc52d63acd5eace49e4f27b77687e64c2780320846aa22df6`.
- [ ] C9 ownership, Landlock, and CUDA support physical hashes remain respectively `4477d45dfc4a0012800610648481186109133f36d49eafaba1b6796d3ca3125b`, `7ab61fe3b33fc3576a2b97196a498d4788926466c10ce8a500e19b57f6490d75`, and `a10abeee487e7264e4ba6f9c273d8fdce4a634e64c9fc3b2957b28d6d58577cf`.
- [ ] C6 receipts remain in the separately pinned superseded-evidence directory and are not used as current qualification.
- [ ] C7 failure artifacts remain preserved and are not cleaned or relabeled into a pass.
- [ ] C8 local-only evidence is not used.
- [ ] B01–B04 and B06–B15 retain the dispositions above.
- [ ] I01–I09 retain the dispositions above.
- [ ] No B05 is invented; no B16+ or I10+ is silently added or omitted.
- [ ] Both canonical r3 runtime files retain their source-inventory sizes and hashes.
- [ ] `pytest==8.4.2` remains qualification-only.
- [ ] `setup_runpod_qualification_guest.sh` is never invoked by final recovery.
- [ ] The authentic 41-file pre-GPU gate passes from final pushed lineage before recovery provisioning.
- [ ] `C9 <= E9 <= F9` is proven with full history.
- [ ] No source/test path differs between C9 and F9.
- [ ] No reviewed-packet path differs between E9 and F9.
- [ ] The v6 review uses the long-context rates and the `$75` ceiling.
- [ ] Historical v4/v5 manifest costs remain immutable.
- [ ] Retrospective values remain `$12.555555` and `$15.121205`, labeled as reconstructions rather than invoices.
- [ ] Original auditor SHA-256 remains `271f4f17a5ed66eaff43dc63f5a02d7ce45cdfd4a3c6a5b5c03bac33cf96a465`.
- [ ] J checkpoint SHA-256 remains `335056c17f0c24053c8c8c1eff168ef49e5a62ca590ffc29c84cf352ecd3ab03`.
- [ ] J metadata remain `n_prompts=125` and `d_model=8192`.
- [ ] Every required map 45–78 is present.
- [ ] Only required maps 45–78 are passed downstream.
- [ ] Scientific J metadata retain `map_count=34`.
- [ ] Extra J maps are recorded only in recovery provenance.
- [ ] Hook boundary, token position, one-token continuation, cache-clone contract, J orientation, final RMSNorm, and LM-head semantics remain unchanged.
- [ ] Primary estimand remains layer 50, dose 0.03, on the exact eight-prompt fixed panel.
- [ ] Prompt-level resampling and the fixed-panel stability-interval label remain unchanged.
- [ ] Layers 51–78 remain descriptive only.
- [ ] No population-generalization, increased-power, or across-layer-selection claim is made.
- [ ] Attempt parent remains `/workspace/csae`.
- [ ] Socket leaf remains `.s`.
- [ ] Maximum, reserve, and budget remain 107, 16, and 91 bytes.
- [ ] Production preflight and execution socket paths remain exactly 91 and 90 bytes.
- [ ] Producer, launcher, and verifier independently reject any path above 91 bytes.
- [ ] Fresh recovery pod is distinct from the C9 qualification pod.
- [ ] Fresh recovery pod independently runs `final_recovery`.
- [ ] Full 45-file/156,023,372,845-byte artifact rehash completes on the fresh pod.
- [ ] At least 1,800 seconds remain before authority is issued.
- [ ] Landlock ABI is at least 4.
- [ ] Policy masks remain `0x7ff2`, `0x1b2`, `0x4002`, and `0x2`.
- [ ] Current-host NVIDIA files are character devices and are bound by path, `st_dev`, `st_ino`, `st_rdev`, major, and minor.
- [ ] No wildcard, device-directory, broader `/proc`, mount, or unconfined fallback is added.
- [ ] Launcher starts single-threaded via direct `-B -E -s -S`.
- [ ] Standard streams are pipes or otherwise pass the unchanged descriptor audit; no writable regular-file redirection is inherited.
- [ ] No protected, canary, NVIDIA, writable regular/directory, unsafe writable-device, `io_uring`, or shared file-backed escape is inherited.
- [ ] Both canary matrices pass with exact errno semantics.
- [ ] Protected canary writable baseline passes before confinement.
- [ ] Output canary is empty after testing.
- [ ] Exclusive Landlock receipt is created only after confinement and consumes authority before `execve`.
- [ ] Same PID survives into the confined bootstrap.
- [ ] Process-lifetime guards remain installed and record zero firings.
- [ ] Attempt marker is exclusive and commit-scoped.
- [ ] Raw and provenance file and directory inventories agree at both endpoints.
- [ ] Endpoint equality is not described as continuous external immutability.
- [ ] No fresh model forward, target render, target feature extraction, or external/prior outcome input occurs.
- [ ] Historical campaign fields remain unchanged.
- [ ] Fresh timing remains under `recovery_execution_campaign`.
- [ ] No compact success is published at or after the recovery deadline.
- [ ] Success compact directory contains exactly audit, summary, and publication marker and no `FAILURE.json`.
- [ ] Every catchable post-marker failure produces the exclusive failure receipt and no scientific success.
- [ ] The exact fresh recovery pod is terminated after retrieval or failure handling.
- [ ] The retrieved bundle passes the standard-library-only offline verifier before any scientific claim is released.

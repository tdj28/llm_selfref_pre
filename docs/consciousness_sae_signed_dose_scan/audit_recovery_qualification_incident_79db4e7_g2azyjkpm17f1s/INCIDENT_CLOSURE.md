# C2 replacement-qualification incident closure

Status: `closed_mechanical_zero_forward_failure_c3_fresh_cycle_authorized`

The consumed C2 qualification on B200 pod `g2azyjkpm17f1s` failed before any
model forward, prompt render, raw-data argument, raw-data open, or scientific
outcome. The receipt records global qualification ordinal 2, successor attempt
1, zero across every forward guard, zero raw-forbidden attempts, one caught
path-guard rejection, and `retry_authorized=false`. The exact owned pod was
deleted and account inventory returned to empty. Conservative receipt-backed
cost was `$0.7070248556583333333333333333`.

The combined v2 message does not by itself prove which source shape/dtype
predicate failed. The bounded diagnosis instead uses the pinned 10,603,226,027
byte checkpoint, its public `torch.HalfStorage` scan, and the pinned producer's
FP16 save default. C3 must reauthenticate all 79 source tensors as FP16
8192×8192 and execute the frozen full layer-45 FP16→BF16 CUDA cast. Separately,
the retained rejected-path hash is exactly SHA-256 of `/proc/self/maps`. C3 may
permit only that exact original decoded pathname in read-only mode after
proving the kernel `self` target. Aliases and every other `/proc/self` path
remain forbidden; the general symlink and raw-path protections are unchanged.

The canonical closure receipt is
`599a712f93fecca1e1007b88a5403de2ed84b76fd6c12c2d273e279e9c979fab`;
independent verification receipt is
`7c080c426e5e1da99b35c1b5c0e2a152b9b98a2f71a2ab12eedca3ab0fed1e2e`.
The incident directory contains the exact outcome-blind equivalence packet,
independent verification, execution receipts, lifecycle evidence, schema, and
stderr needed for replay.

After this failure, the user explicitly stated: “Authorize C3 and augment the
experiment skill with lesson learned”. The incident closure does not turn C2
into a pass. Fresh authority is defined only by `RECOVERY_CYCLE_LEDGER_V3.json`
and its non-circular successor-authority binding.

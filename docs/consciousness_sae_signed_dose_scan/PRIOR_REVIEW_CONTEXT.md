# Compact prior-review context for the signed dose scan

This is design provenance, not an analysis input and not a raw-data packet.

The immediately preceding target-blind calibration/recovery received a single
terminal `READY TO FREEZE` review from `gpt-5.6-sol` in Pro mode (response
`resp_0cd2aad98e3cdd53016a593cab139081989347eaeaaca831c6`). Its final review
resolved the enumerated blockers and froze layer 50 as the sole confirmatory J
source; layers 51-78 were descriptive. The predecessor result summary is
hash-bound in this successor's design provenance.

The completed calibration established only numerical facts relevant to this
new design:

- requested-direction delivery failed at 1% in all 24 rows but passed at the
  prespecified 2%, 3%, 4%, and 8% anchors;
- the actual final-state response over 2%, 3%, and 4% was nonlinear;
- the learned J beat random-J in the checked contrast but did not establish
  residual-space added value over identity; and
- those observations motivated, but do not enter, this new exploratory curve.

No predecessor row, raw tensor, random direction, token panel, random-J draw,
or runtime seed is loaded, pooled, or analyzed in the successor. The new scan
is not confirmatory and cannot inherit claims from the earlier run.

The present review is a single successor-design review. It should not reopen
resolved predecessor implementation history unless a concrete flaw affects
the new protocol.

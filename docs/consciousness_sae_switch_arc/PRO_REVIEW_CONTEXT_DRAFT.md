# Review context: readout-validation pilot and SAE switch-arc successor

Status: target-outcome empty draft, 2026-07-13.

The reviewer must also read `DESIGN_EXPOSURE_LOG.md`. The program is
computationally isolated from prior outcomes but is not represented as if its
designers had no prior domain or historical-result exposure.

## Requested review

Review the investigative `consciousness_readout_validation_v1` pilot and the
dependent confirmatory `consciousness_sae_switch_arc_v1` protocol as one ordered
program. Be adversarial about:

1. whether each positive control validates only what it claims;
2. whether clean semantic clozes and finite-difference transport make a later
   target null scientifically interpretable without circularity;
3. distribution shift from diagnostic clozes/neutral prompts to free-running
   self-reference text;
4. exact causal timing, cache isolation, and text-carryover interpretations;
5. target, matched-SAE, isotropic, identity, and random-J controls;
6. the claim ledger, multiplicity, equivalence, power, and judging;
7. the checkpoint-only archive and arbitrary-vocabulary replay contract;
8. whether any pilot result could improperly tune or rescue the confirmatory
   target study; and
9. the smallest decisive repairs required before a freeze.

Do not suggest weakening a prospective failure after seeing it. Do not treat
the pilot as evidence about consciousness or the target causal effect. Distinguish
fatal flaws, required pre-freeze repairs, useful secondary additions, and
non-blocking limitations.

## Terminal predecessor

`consciousness_sae_changepoint_v1` stopped before target execution. Two
literal endpoint controls failed and its one allowed prospective amendment
then failed the pre-edit vector-RMS safety gate. The terminal receipt SHA-256 is
`d10205c1ec4beb4b84e1cc0126bbe7d21c543aa0ad8ac8ee1f73e24580667140`;
the completed transaction manifest SHA-256 is
`4f62bfb8534ec77bda46c8c9175e6dcadc6995781f5723c5ea68105b185d788f`.
No exact-paper target generation, activation, judgment, or endpoint was opened.

The predecessor is not being rescued. Its autolabel-selected IDs `3415`,
`4042`, and `4752`, multiplier `5.128`, matched-feature assignments, seeds,
machine plan, power/benchmark results, and receipts are prohibited successor
inputs. The six authors' later-public target feature IDs and immutable public
artifact revisions are source facts and are independently rehashed.

## Why the old semantic control was rejected

The three coordinates came from unscored public autointerpretation wording and
were evaluated using nearly the same vocabulary. They lacked adjacent-family
hard negatives and coordinate-scale comparability. Feature `4752` is broadly
about awareness rather than a validated self-awareness feature. The amendment
combined heterogeneous decoder columns and applied an unrelated multiplier,
then correctly failed before editing because its vector exceeded 10% of clean
layer-50 RMS. That result cannot be attenuated or retried under its study ID.

## Replacement pilot

The separate pilot has five narrow gates:

- `G1`: every J map from 45 through 78, independently implemented arithmetic,
  orientation, shape, finiteness, and neutral-token selected logits;
- `G2/G2b`: symmetric small random perturbations at every source layer,
  comparing actual central finite differences with real J, identity, and five
  random-J transports, including 1%/2% linearity anchors;
- `G3`: no intervention; 72 independently authored clozes crossing nine
  semantic families and two render modes, with full confusion, adjacent-family
  discrimination, leave-one-token-out tests, actual-final grounding, and
  identical-statistic random-J baselines;
- `G3P`: 24 trivial balanced factual `Yes`/`No` questions at the exact
  answer-predicting position; and
- `G4`: preflight every planned target/matched/isotropic BF16 vector against all
  clean neutral layer-50 RMS values before any edit, followed by disjoint
  sentinel telemetry only if the preflight passes.

No SAE autolabel selects or validates the semantic endpoint. `G1/G2` validate
arithmetic/transport, `G3` validates a deliberately diagnostic clean readout,
`G3P` validates polarity parsing/position, and `G4` validates numerical safety.
None establishes that a target SAE is a consciousness direction.

## Confirmatory successor

The final target study uses the exact paper self-reference induction and binary
query with the pinned public Llama 3.3 70B model, native layer-50 public SAE,
and every available J map from 45 through 78. A shared 96-token clean prefix is
forked into never, sham, target plus/minus, freshly matched plus/minus, and
norm-matched isotropic plus/minus branches. The clean prefix/cache is never
rerendered under an active hook.

The primary causal event is the first next-token distribution computed with an
active hook. Natural text, disposable query-predictor logits, and a compact
fixed-token assay are aligned to that event. All registered positions retain
layers 45–49, `50_pre`, `50_post`, layers 51–78, and the actual final pre-norm
residual/logits.

The claim ledger separates natural stance, target final-answer risk difference,
target-minus-matched final-answer specificity, query-conditioned report
polarity, and pre-query explicit-consciousness topic. The bundled mechanism
requires all five plus the deception/roleplay manipulation gate and each
endpoint's assigned technical/semantic/reliability gates. J endpoints use the
full 51–78 depth AUC and an actual-final-logit component; immediate geometry or
a favorable layer cannot substitute.

## Cost-safe archive

The de-duplicated candidate checkpoint-only matrix has 80 positions per
transcript block and 36 source states per position. At candidate `N=640`, exact
BF16 residuals are 28.125 GiB. Raw residuals plus the pinned
maps/norm/head/tokenizer make the
full vocabulary and arbitrary top-K (including 2,000) replayable, so the run
does not duplicate packed top-K arrays. The candidate sample size is not frozen
until same-design power passes.

## Model and review pricing verification

Official OpenAI documentation was checked on 2026-07-13. The latest flagship is
`gpt-5.6-sol`; `gpt-5.6` routes to it. Pro is requested with
`reasoning.mode="pro"`, not a separate model slug. Standard API pricing per one
million tokens is `$5.00` uncached input, `$0.50` cached input, `$6.25` cache
write, and `$30.00` output. The canonical review script must first run in
`--dry-run` mode, verify the latest model, and enforce a conservative budget
authorization before the single paid request.

## Hard gates after review

The paid review and its adjudication do not authorize target execution. The
final design must still pass local/synthetic tests, pilot execution, independent
audit, same-design power, and an exact measured workload benchmark. The final
freeze commit is pushed first. The responsible human then separately approves
the exact immutable embargoed OSF registration and the exact measured spend
ceiling. Only those bound receipts unlock the target CLI.
